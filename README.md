LibraryBookTracker is a Java command-line application used to manage a simple library catalog stored in a text file.
The program supports searching books by title or ISBN and adding new books while validating data and logging errors.
